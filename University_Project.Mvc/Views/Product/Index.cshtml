@model Product

@{
    ViewData["Title"] = "product Page";
}

<main>
    <div class="product-container">
        
        <div class="left-column">
            <div class="image-container">
                @{
                    var base64 = Convert.ToBase64String(Model.Image);
                    var imgSrc = String.Format("data:image/gif;base64,{0}", base64);
                }

                <img src="@imgSrc" />
            </div>
            <div class="slider-container">
                <div class="slider">

                </div>
            </div>
        </div>
        <div class="right-column">
            <div class="product-header">
                <header>
                    <h1>@Model.Name</h1> 
                    <h4>@Model.Category_Id</h4>
                    <h3 class="space" style="height: 45px;"></h3>
                </header>
                    <p class="product-description">
                        @Model.Description
                    </p>
            </div>
            <div class="product-footer">
                <footer>
                    <p>@Model.Price$</p>
                    <a asp-controller="CartItem" asp-action="CreateCartItem" class="btn"> Add To Cart</a>
                </footer>
            </div>
        </div>
    </div>


    @*<div class="product-container">
        <div class="left-column">
            <div class="image-container">
                <img src="~/images/blog-1.jpg">
            </div>
            <div class="slider-container">
                <div class="slider">

                </div>
            </div>
        </div>
        <div class="right-column">
            <div class="product-header">
                <header>
                    <h1>@Model.Name</h1> 
                    <h4>@Model.Category_Id</h4>
                    <h3 class="space" style="height: 45px;"></h3>
                </header>
                    <p class="product-description">
                        @Model.Description
                    </p>
            </div>
            <div class="product-footer">
                <footer>
                    <p>@Model.Price</p>
                    <a asp-area="" asp-controller="Catalog" asp-action="ProductAddPage" class="btn">read more</a>
                    <a asp-area="" asp-controller="CartItem" asp-action="CreateCartItem" class="btn"> Add To Cart</a>
                </footer>
            </div>
        </div>
    </div>*@
</main>